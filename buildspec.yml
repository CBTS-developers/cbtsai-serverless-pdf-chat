version: 0.2

phases:
  install:
    runtime-versions:
      # nodejs: 18
      python: 3.11

  build:
    commands:
      - cd backend
      - sam deploy --stack-name serverless-pdf-chat --resolve-s3 --parameter-overrides 'ParameterKey=Repository,ParameterValue=CBTS-developers/cbtsai-serverless-pdf-chat'

artifacts:
  files:
    - "**/*"